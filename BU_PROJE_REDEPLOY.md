# 🚀 Bu Proje İçin Redeploy Komutları

## 📍 Proje Bilgileri

- **Klasör:** `Arhaval Denetim Merkezi`
- **Git Remote:** `origin` → `https://github.com/arhaval/yonetim.git`
- **Branch:** `main`

---

## ✅ Bu Proje İçin Komutlar

### 1. Bu Proje Klasörüne Git

```bash
cd "C:\Users\Casper\Desktop\Arhaval Denetim Merkezi"
```

veya Git Bash'te:

```bash
cd ~/Desktop/"Arhaval Denetim Merkezi"
```

---

### 2. Değişiklikleri Kontrol Et

```bash
git status
```

---

### 3. Tüm Değişiklikleri Ekle

```bash
git add .
```

---

### 4. Commit Yap

```bash
git commit -m "Fix: Reports page error handling and add error handling to all pages"
```

---

### 5. GitHub'a Push Et (Bu Proje İçin)

```bash
git push origin main
```

---

## 🚀 Tek Seferde (Kopyala-Yapıştır)

```bash
cd "C:\Users\Casper\Desktop\Arhaval Denetim Merkezi"
git add .
git commit -m "Fix: Reports page error handling and add error handling to all pages"
git push origin main
```

---

## 📝 Diğer Projeye Geçmek İçin

Eğer "e pick em" projesine geçmek isterseniz:

```bash
cd "C:\Users\Casper\Desktop\e pick em"
```

veya başka bir projeye:

```bash
cd "C:\Users\Casper\Desktop\PROJE_ADI"
```

---

## ⚠️ Önemli

- **Bu komutlar sadece "Arhaval Denetim Merkezi" projesi için!**
- Diğer projeye geçmeden önce bu komutları çalıştırın
- Her proje için ayrı klasör ve ayrı git repo var

---

**BU PROJE İÇİN BU KOMUTLARI KULLAN!** 🎯

